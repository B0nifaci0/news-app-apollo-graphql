/**
 * @format
 */

import { AppRegistry } from 'react-native';
import HolaMundo from './src/components/HolaMundo';
import { name as appName } from './app.json';

AppRegistry.registerComponent( appName, () => HolaMundo );
