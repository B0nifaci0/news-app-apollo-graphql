import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const Noticias = ( { children, style, ...rest } ) => (

  <View style={style}>
    {children}
  </View>
);

export default Tab1;
